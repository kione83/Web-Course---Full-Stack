// Import your external javascript file into your html file using the `<script>` tags, making sure the `src` is correctly linking to the .js file.

// Change the following elements using ONLY JavaScript and the DOM methods.

// pseudocode - write one line of comments for every line of code.

// 1. Change the inner text of the first `<h1>` element to “I’m using JavaScript!!”.

//use document.getElementById with the id of `title`, then access innerText to change the text that is displayed.

// 2. Change the background color of the first `<div>` with the id of `animalContainer` to blue.

// 3. Give both of the `<div>` elements a black border by using the document.getElementById query selector and setting them to the appropriate corresponding id.
